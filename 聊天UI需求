桌宠聊天 UI 界面需求规格说明
1. 整体概述
该模块为桌宠应用中的主聊天浮窗界面，以非侵入式悬浮窗形式常驻桌面，呈现一个圆润可爱、色彩明快的横向长方形对话框。整体风格偏向轻量、软萌，与“鲸鱼娘”桌宠角色形成统一视觉体系。

2. 界面布局与尺寸
整体形状：横向长方形，宽度明显大于高度，比例建议约 3:2 或 16:9（如宽度 480px，高度 300px 左右），可根据实际美术资源微调。

窗口类型：无标准标题栏的异形悬浮窗，支持鼠标拖拽移动，层级置于普通窗口之上但不强制置顶（允许用户点击穿透区域设置）。

内容区域：内部为对话消息展示区 + 可能的输入组件（若需输入功能），本次仅关注整体容器样式。

3. 视觉样式详细规范
3.1 边框
颜色：明亮且饱和度适中的天空蓝 / 淡海蓝，如 #4A9EFF 或 #5CA0F2，与鲸鱼娘角色主色调呼应。

宽度：15px（约十至十六像素范围，取 15px 作为基准值，保证视觉分量感）。

样式：实线，无渐变，可略带轻微内阴影以增强立体感，但不做拟物化浮雕效果。

3.2 圆角
外边框圆角：半径 30px，确保所有拐角均呈现平滑的圆弧过渡，整体形状接近“超椭圆”观感，消除锐利边角。

内部填充区圆角：与外边框匹配，视觉上边框为均匀宽度的圆角描边。

3.3 背景
内部填充色：纯白色 #FFFFFF，干净简洁，便于显示黑色或深灰气泡文字。

边框与白色背景之间：无额外间隙，白色区域直接延伸至边框内缘。

3.4 阴影
浮窗整体添加柔和投影：0px 8px 24px rgba(0, 0, 0, 0.12)，增强悬浮层次感，不干扰白色背景清爽度。

4. 右上角预留区域（鲸鱼娘趴姿动画）
4.1 位置与形状
在窗口右上角边框区域外侧/交接处，刻意留出一块视觉缺口或叠层空间，用于放置鲸鱼娘“趴姿”动态角色。

实现方式建议：

方案A（推荐）：将桌面宠物角色作为独立透明窗口，始终吸附或跟随主聊天窗口右上角，并覆盖边框上方，形成角色趴在边框上的效果。

方案B：在主窗口右上角使用异形裁切（如 border-radius 结合伪元素镂空），边框在该处断开，留出约 60×60px 的角色放置区，角色由动画层单独渲染。

4.2 动画需求
角色以趴姿依附在边框上沿，身体自然垂下，鳍或尾巴偶尔摆动。

支持循环待机动画（呼吸、轻微晃动、眨眼睛），无操作时持续播放。

当有新消息或用户点击时，可触发短互动动画（如抬头、拍打边框），动画结束后回归趴姿。

5. 交互与行为
拖拽移动：鼠标按住聊天窗口任意空白区域（除输入框、按钮等控件外）可拖动整个窗口。

点击角色：点击右上角鲸鱼娘角色可唤出菜单或触发互动表情，不干扰聊天区域操作。

缩放适配：暂不支持等比缩放，固定尺寸；后续若有需求可提供多个预设尺寸档位。

层级关系：聊天窗口与桌宠角色主体窗口保持紧耦合，一同移动、一同隐藏/显示。

6. 技术实现建议（仅作参考）
框架选择：Electron + HTML/CSS（Web技术实现圆角边框、阴影和动画极为便捷），或使用 Unity 2D 制作更丰富的角色骨骼动画。

边框实现：使用 border: 15px solid #4A9EFF; border-radius: 30px; 搭配透明背景的窗口裁剪实现圆角悬浮窗；右上角角色通过独立 canvas 或 webp/lottie 动画层叠展示。

异形窗口：若需让角色真正“趴”出窗口边界，可开启 transparent 和 frame: false 模式，再叠加角色层。

